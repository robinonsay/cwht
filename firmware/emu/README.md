# firmware/emu (placeholder)

Emulation scenarios for the optional Emulation evidence class (07 sections 1.2 and 9.4;
04 section 4). Empty until the emulator ADR due at PDR is Accepted; that ADR fixes the
harness, vendoring form and scenario file format. Scenarios assert event order in TIMER0
microseconds only, never durations (ADR-011).
