---
# Peer-review record front matter (charter section 5; docs/process/01-lifecycle-and-reviews.md section 13
# is the single field list; docs/process/08-agent-briefing.md section 3.2; template
# docs/templates/peer-review-checklist-requirements.md revision C, section G and item A8, the checklist
# 08 section 3.1 routes "plans and process documents" to).
# This is the separate software assurance record of the Software Development and Management Plan
# (SWE-013; 07 section 2.1.1 row "Software plans": Yes in every column, because 07 is also the software
# assurance plan and the software safety plan, NPR 7150.2D 6.1 items k and l). The paired peer review of
# the same blobs is INSP-010, docs/reviews/SRR/checklists/software-plan-07.md (reviewer:software-plan).
id: INSP-018
checklist: peer-review-checklist-requirements
checklist_revision: C
checklist_file: docs/reviews/SRR/checklists/software-plan-07-software-assurance.md
product: docs/process/07-software-engineering-plan.md
# product_commit: git blob (git hash-object) of the reviewed working-tree 07 (revision A.3; last commit
# touching it 4e3f891; HEAD 28e49e6; not yet committed, SRR package section 2 item H17). Both product
# blobs are in product_files; they equal the blobs INSP-010 reviewed.
# iteration 2 verified the fixed blobs below (07 revision A.4 working tree, 1007 lines; measurements.json 89 records);
# iteration 1 reviewed 07 blob 1b8864b0121f3f8547e1b194d63b41752ab946b9 and measurements.json blob 9bb5989e7080c55e8c97d82e64256a07f9b8788f.
product_commit: "eba15bcb8ab0cecfd8bc155bd749bc89fdcddb34"
product_files: ["docs/process/07-software-engineering-plan.md@eba15bcb8ab0cecfd8bc155bd749bc89fdcddb34", "docs/plan/measurements.json@5e2d1755c6ad43d1c3cdf6fae0ff0edbfeb7346b"]
# inputs read (not reviewed): hazards.json 0.4.0-pha, 03 third revision, rmm.json, hazard-analysis.md 0.4.0-pha
input_files: ["docs/safety/hazards.json@89d0cbc323b2775d326cf7cd42bc637818ec96eb", "docs/process/03-software-classification-and-rmm.md@c87abe5a09985c3f0a3adb74e17386cc61d731b4", "docs/process/rmm.json@3645b4f682cba38373f0fca55beb190ded36c032", "docs/process/03-software-classification-and-rmm.md@fdc8d0764e7ee513a05215de7dfb580687b721d4 (iteration 2)", "docs/process/rmm.json@78c3b2364473e0676f8b5405ecd9f8536715bea1 (iteration 2)", "docs/safety/hazard-analysis.md (0.4.0-pha, working tree)"]
product_size: 07 revision A.3, 23 sections and annexes A to D, 960 lines; measurements.json 47 records (MSR-01 to MSR-28; 32 Measured, 15 Not yet measured)
sprint: SRR-prep
author_agent: "author:software-plan (Claude software lead; 07 revision A.3 of 2026-09-25 and the SRR seed of measurements.json, SRR items H14 07 part and H16 SWE-089)"
reviewer_agent: "sa-reviewer:software-plan"
criticality: safety-critical
assurance_required: true
# assurance_reviewer_agent: this record IS the software assurance review; its reviewer is
# sa-reviewer:software-plan, distinct from the author and from the file reviewer of the paired record
# INSP-010 (reviewer:software-plan). See finding-3 on how 07 section 10.2 does not yet provide for a
# separate assurance record.
assurance_reviewer_agent: "sa-reviewer:software-plan (software assurance function; paired peer review INSP-010 by reviewer:software-plan)"
paired_record: INSP-010
iteration: 2
# iteration 2 (2026-09-26): all seven findings Verified; reviewer_verdict and assurance_verdict APPROVED for the
# assurance review; record verdict held at NEEDS CHANGES by the 07 section 10.2 pairing rule until INSP-010 is
# APPROVED, and readiness R1 (measurements schema, INSP-010 finding-2) still fails. items_no is the iteration 1 list.
# readiness_met: false: R1 fails (measurements.json has no schema; INSP-010 finding-2) and R3 (the author
# self-check against sections A to G) was not supplied to this reviewer
readiness_met: false
reviewer_verdict: APPROVED
assurance_verdict: APPROVED
verdict: NEEDS CHANGES
findings_major: 2
findings_minor: 5
findings_open: 0
findings_fixed: 0
findings_verified: 7
findings_deferred: 0
assurance_findings_major: 2
assurance_findings_minor: 5
assurance_tasks_applied: [swe-013 7.1 task 1, swe-013 7.1 task 2, swe-022 7.1 task 1, swe-033 7.1 task 1, swe-033 7.1 task 3, swe-089 7.1 task 1, swe-134 7.1 task 1, swe-134 7.1 task 4, swe-134 7.1 task 5, swe-134 7.1 task 6, swe-205 7.1 task 2, swe-205 7.1 task 3]
deferred_rids: []
items_no: [R1, R3, CK-REQ-G1, CK-REQ-G3, CK-REQ-G4, CK-REQ-G6, CK-REQ-G7, SA-013-1, SA-013-2, SA-089-1, SA-134-1, SA-134-4, SA-134-6, SA-220-1]
effort_turns: 72
effort_minutes: 100
record_status: Open
date: 2026-09-25
date_closed: null
---

# Peer review record INSP-018: software assurance review of the Software Development and Management Plan (07) and the measurement repository

**Products.** `docs/process/07-software-engineering-plan.md` revision A.3 (working tree on HEAD `28e49e6`, blob `1b8864b0121f3f8547e1b194d63b41752ab946b9`, last commit touching it `4e3f891`) and `docs/plan/measurements.json` (untracked, blob `9bb5989e7080c55e8c97d82e64256a07f9b8788f`), reviewed as one product, the same blobs INSP-010 reviewed. **Sources of record read:** `docs/safety/hazards.json` `0.4.0-pha` (blob `89d0cbc3...`), `docs/safety/hazard-analysis.md` sections 6 and 7, `docs/process/03-software-classification-and-rmm.md` sections 4.3 and 5 (blob `c87abe5a...`), `docs/process/rmm.json` (blob `3645b4f6...`), `docs/requirements/sys/requirements.json`, the SRR records in `docs/reviews/SRR/checklists/`.

**Checklist.** `docs/templates/peer-review-checklist-requirements.md` revision C: readiness R1 to R5, section G (CK-REQ-G1 to G8) and CK-REQ-A8, the "plans and process documents" row (08 section 3.1; 07 section 10.1 row b). `docs/templates/peer-review-checklist-software-assurance.md` does not exist (08 section 3.5: due before PDR), so, per 07 section 15, the SWEHB section 7.1 software assurance tasks of the SWEs this plan implements are applied directly and listed below as items SA-NNN-n. **Minimum content judged:** SRR entrance row 23 (01 section 4.3, "Software plans (preliminary)", NPR 7150.2D chapter 6 records 6.1 a, k, l, t, u), SWE-013 with its assurance tasks, and the safety-critical provisions SWE-134, SWE-205, SWE-219, SWE-220 as planned in 07 section 14. **SRR context:** `docs/reviews/SRR/package.md` section 2 items H1 (07 record with a software assurance reviewer), H14 (07 section 14 reconciliation; SWE-033 trade study) and H16 (SWE-089 measurements).

**Search-first compliance.** `mcp__claude-context__search_code` on `/Users/robinonsay/rust/cwht` ran before any manual search (queries: NPR 7150.2D chapter 6 SDP content and SWE-013; SWE-134 items a to l; SWE-033 assurance tasking; SWEHB 5.17 minimum content; partitioning and isolation of safety-critical elements). The tool was available. `grep -n`, `sed -n` and read-only Python over the JSON files were used afterwards only to pin lines and recompute values.

**Independence.** This reviewer did not author 07, `measurements.json`, `hazards.json`, `hazard-analysis.md` or 03, is a different invocation from the INSP-010 reviewer, and edited none of the product files. INSP-010 was read after this reviewer's own checks of 07 sections 2, 10, 11, 14 and 15 were done; the section "Concurrence with INSP-010" records the result.

## Findings

Ids follow the `finding-<n>` anchor rule of 01 section 13 (the assignment's F-nn label in brackets). Severity: Major blocks the baseline; Minor is fixed before the next review. At iteration 1 all findings were Open; the iteration 2 closure verification (section "Closure verification") set every one to Verified and filled the Disposition column.

| Finding | Origin | Severity | Item | Location | Description | State | Deferred to | Disposition |
|---|---|---|---|---|---|---|---|---|
| <a id="finding-1"></a>finding-1 (F-01) | assurance | Major | SA-134-4, SA-134-1, CK-REQ-G1 | 07 section 14.1 "Neither" paragraph ("menu handling and LCD content other than the frequency display and fault annunciation"); section 14.2 row d; section 14.2 module rows `SW-TXSEQ` (d), `SW-KEYER` (d), `SW-AUDIO` (d); section 5 item 1 (architecture content); section 7 (no isolation rule) | Row d makes every SWE-134 d operator override of a hazard control (5 W step REQ-SYS-063, tune above 0.5 W REQ-SYS-019, audio cap unlock REQ-SYS-074, guest-lock release REQ-SYS-066, Straight-on-tip REQ-SYS-056, bench test mode REQ-SYS-179) "a menu action (or the held combination) followed by a separate confirmation", and section 14.1 classes menu handling Neither. The plan therefore rests a safety-critical provision of `SW-TXSEQ`, `SW-KEYER` and `SW-AUDIO` on events generated by non-safety-critical code, and nowhere states that the receiving component validates the two actions itself or that non-safety code cannot write safety-critical state. More generally the plan has no partitioning or isolation provision: section 5 item 1 lists the architecture content without an isolation requirement; section 7 has no rule confining safety-critical state (PA-permit flag, power step, cap state, guest lock) to its owning module (for example private fields reachable only through the safety-critical module's checked API) and does not consider the Cortex-M33 MPU; everything is in the single crate `cwht-core` (section 1.2). SWEHB `swe-134-safety-critical-software-design-requirements.md` section 7.1 task 4 asks the assurance function to confirm "use of partitioning or isolation methods in the design and code" and that "the design logically isolates the safety-critical design elements and data from those that are non-safety-critical"; SWEHB `swe-023` section 7.4.1 Step 2 repeats it. A menu fault that emits action and confirmation from one gesture (encoder bounce, stale queue entry, corrupted menu state) defeats SWE-134 d for HZ-001, HZ-004, HZ-005 and HZ-006. The same gap was raised against 03 by INSP-017 finding-1 (Major); this plan carries the allocation and the coding standard, so it is raised here independently. Fix: (1) add to section 14.2 row d that each receiving safety-critical component accepts an override only from two distinct operator events it validates itself (distinct input samples and a minimum interval, both from the 1 kHz SIO sampling of CS-35), so that no menu fault alone produces an override, and add the matching HostUnit case (faulty menu emitting both events) to the row d Verification cell; (2) add a partitioning rule to section 5 item 1 and a CS rule in section 7 (safety-critical state private to its module, written only through that module's checked functions; MPU use decided by the PDR architecture ADR); or (3) class the override command path of menu handling safety-critical in 14.1 with the owner's ruling as SMA TA (package decision 9 context), consistent with the 03 disposition of INSP-017 finding-1 | Verified | | Closed (iteration 2, verified 2026-09-26). All three fix options applied. (1) 07 section 14.2 row d: the receiving component (`SW-TXSEQ`, `SW-KEYER`, `SW-AUDIO`) accepts an override only from two distinct operator events it validates itself from the CS-35 1 kHz samples, a menu output is an untrusted request (CS-39), and the Verification cell adds the faulty-menu HostUnit case per receiving component (one input sample and one queued event). (2) Section 5 item 5 (partitioning and isolation, citing SWEHB `swe-134` 7.1 task 4 a and b, pinned at lines 155 and 157 of the page) and new section 7.8 rule CS-39 (safety-critical state private to its owning module, changed only through checked functions; MPU decision to the PDR architecture ADR). (3) Section 14.1 Proposed row "Menu override command path" (criterion a; HZ-001, HZ-004, HZ-005, HZ-006; 03 item X20) with its 14.2 module row (a, d, e, g, i, k), the Owner decision paragraph stating that row d and CS-39 apply in both outcomes, and the Neither and mission-critical paragraphs matching 03 section 4.3 (03 blob `fdc8d07`). Ruling tracked in 07 section 22 row "Menu override command path ruling" |
| <a id="finding-2"></a>finding-2 (F-02) | assurance | Major | SA-134-1, SA-134-6, CK-REQ-G1 | 07 section 14.2 paragraph after the twelve-row table ("the union is never smaller than the allocation of the `hazard-analysis.md` section 7 Components column that 03 section 5 transcribes"; "the clock driver and the mission-critical rows carry the items this plan allocates"); module rows `cfg_guard`, `SW-CFG`, `SW-DISPLAY`, `SW-SYNTH` fallback sentence; `rmm.json` row SWE-134 ("the mission-critical allocation follows the 07 section 14.2 module row") | Recomputed on 2026-09-25 against 03 section 5 (the allocation that 03 section 5 and 07 section 14.2 itself name as governing under the 03 order of precedence) and `hazard-analysis.md` section 7 Components column. Safety-critical: the configuration guard `cfg_guard` carries a, b, c, f, g, h, k, l but 03 section 5 row e and `hazard-analysis.md` section 7 row e allocate e to the configuration guard ("configuration write sequence"); 07's own rule "never smaller" is broken. Mission-critical: 03 section 5 allocates a, b, e, g to the configuration store and non-safety fields, 07 `SW-CFG` carries a, f, k (missing b, e, g); 03 allocates a, b, g to frequency display and encoder input, 07 `SW-DISPLAY` carries g, k (missing a, b); 03 allocates a, b, e, g to frequency control outside the word path, 07 has no module row for those `SW-SYNTH` units and its decision-9-declined fallback carries b, g, h, k (missing a, e). Two baseline candidates therefore give different SWE-134 item sets for the same units, and the RMM SWE-134 row points to 07 while the 03 precedence rule points to 03. The consequence reaches PDR: the requirements checklist item CK-REQ-C4 checks the module files against the 07 second table, so the missing items would not be written or checked. SWEHB `swe-134` section 7.1 task 1 ("work with the project to implement NPR 7150.2 requirement items a through l") and task 6 ("the SWE-134 implementation supports and is consistent with the system hazard analysis"). Fix: make each 07 module row a superset of the 03 section 5 allocation (add e to `cfg_guard`; b, e, g to `SW-CFG`; a, b to `SW-DISPLAY`; a row for the mission-critical `SW-SYNTH` units with a, b, e, g and a, e in the fallback sentence), or change 03 section 5 in the same change set with the reason; then state one governing source for mission-critical allocation in 07, 03 and the RMM SWE-134 row (cross item) | Verified | | Closed (iteration 2, verified 2026-09-26). Script-free item-by-item comparison of every 07 section 14.2 module row with 03 section 5 (blob `fdc8d07`) Safety-critical and Mission-critical columns: every row is now a superset. `cfg_guard` a, b, c, e, f, g, h, k, l (e added); thermal unit a, d, g, h, j, k, l (a, k added); `SW-CFG` a, b, e, f, g, k (03: a, b, e, g, k); `SW-DISPLAY` a, b, g, k with the selection path (03: a, b, g, k for both); new mission-critical `SW-SYNTH` row a, b, e, g, k (03 frequency control: a, b, e, g, k); decision-9 fallback a, b, e, g, h, k; `envelope` and `alc` b, g, h, k (03: b, g, h, k); menu path a, d, e, g, i, k (03: a, d, e, g, i, k); frequency-word path and verification unit supersets. One governing source stated in the 14.2 paragraph after the table (07 module rows for mission-critical; 03 section 5 plus the hazard union as the floor for safety-critical); 03 section 5 lead-in and `rmm.json` SWE-134 implementation ("the mission-critical allocation follows the 07 section 14.2 module row", blob `78c3b23`) say the same |
| <a id="finding-3"></a>finding-3 (F-03) | assurance | Minor | CK-REQ-G3, SA-013-1, SA-089-1 | 07 section 2.1 row "Software assurance reviewer" ("written into the same record (section 10.2)"); section 2.1.1 lead paragraph; section 10.2 rows "Completion criteria" and "Record" ("the software assurance reviewer adds its verdict, findings and applied SWEHB tasks ... to the same file"); section 15 table row "5.17 items 1 to 3"; section 11.1 rule "The current value of a measure for a scope is its last record for that `id` and `scope`" with MSR-20 and MSR-21 `scope` set to the product path | The SRR practice files the software assurance review as a separate record (INSP-017 for 03 beside INSP-009; this INSP-018 for 07 beside INSP-010), which 07 does not provide for: no slug rule, no field linking the pair, no rule for how the software lead derives the product's record `verdict` from `assurance_verdict` held in another file, and INSP-010 still reads `assurance_reviewer_agent: assurance:software-plan (not yet dispatched)` with `assurance_verdict: pending`. The measurement rule breaks too: two MSR-20 records with the same `scope` (the product path) for INSP-010 and INSP-018 make the later one the "current value" and silently drop the other's findings from the SWE-089 roll-up. Fix: either (a) state in sections 2.1, 10.2 and 15 that the assurance review may be a paired record `checklists/<product-slug>-software-assurance.md` naming the paired INSP id, that the product record's `verdict` is set only when both are APPROVED, and that INSP-010 names this record in `assurance_reviewer_agent` and carries its verdict; or (b) merge this record into INSP-010 by a recorded decision; in both cases key MSR-20 and MSR-21 on the INSP id (add it to `scope` or a new field in the section 11.1 table and the schema) | Verified | | Closed (iteration 2, verified 2026-09-26). 07 section 2.1 assurance reviewer row and section 2.1.1 lead paragraph name the paired assurance record; section 10.2 Completion criteria (the product record's `assurance_verdict` is the paired record's; neither record APPROVED until both reviews are) and Record row (slug `<product-slug>-software-assurance.md`, own INSP id, `paired_record` field, findings counted once in 11.3 trends); section 15 rows 5.17 items 2 and 3 and item 6. Section 10.3 and 11.1 key MSR-05, MSR-20 and MSR-21 on the INSP id at the start of `scope`, with the re-key rule for `supersedes`. The `paired_record` field in 01 section 13 and `tools/validate_docs.py`, and the INSP-010 and INSP-009 front matter, are carried in 07 section 22 row "Paired assurance record fields" (before the SRR readiness declaration) |
| <a id="finding-4"></a>finding-4 (F-04) | assurance | Minor | SA-089-1, CK-REQ-G6 | `measurements.json` MSR-20 and MSR-21 records; 07 section 10.3 roll-up paragraph ("one MSR-20 record ... and one MSR-21 record ... per `INSP-NNN` record at every sprint closure and at every gate package") | SWEHB `swe-089` section 7.1 task 1: "Confirm that the project records the software peer reviews and results of software inspection measurements." Recomputed on 2026-09-25 from the record front matter: (a) the evidence `sha256` of the MSR-20 and MSR-21 records for `docs/safety/hazard-analysis.md` (INSP-008) and `docs/risk/register.json` (INSP-007) no longer matches the records; INSP-008 now reads major 3, minor 12, open 3, verified 12, items_no 3, 74 turns, 90 min (seed: 3, 10, 13, 0, 16, 46, 55) and INSP-007 reads major 4, minor 12, open 2, fixed 14, items_no 2, 72 turns, 110 min (seed: 4, 11, 15, 0, 12, 48, 70); (b) six filed records have no MSR-20 or MSR-21 record: INSP-003 (`requirements-sys.md`), INSP-004 (`requirements-tx-and-sw-keyer.md`), INSP-009 and INSP-017 (classification), INSP-010 (this product) and INSP-012 (ICD stubs), three of which are 07 section 10.1 software products (INSP-004 for `SW-KEYER`, INSP-009 and INSP-017 for 03); (c) the MSR-20 value objects of INSP-007 and INSP-008 omit `assurance_major` and `assurance_minor`, which the other eight carry. The seed is a dated snapshot and the file is append-only, so this is not an edit request. Fix: at the SRR package build append correction records with `supersedes` for (a), new records for (b) and for this record, and the two keys for (c) (section 11.1 rules) | Verified | | Closed (iteration 2, verified 2026-09-26). `docs/plan/measurements.json` (blob `5e2d175`, 89 records, `updated` 2026-09-26) holds re-keyed MSR-20 and MSR-21 records dated 2026-09-26 for INSP-001 to INSP-014, INSP-016, INSP-017 and INSP-018, each `scope` starting with the INSP id: (a) INSP-007 now 4, 12, open 2, fixed 14, items_no 2, 72 turns, 110 min and INSP-008 3, 12, open 3, verified 12, items_no 3, 74 turns, 90 min, both with `supersedes`; (b) INSP-003, 004, 009, 010, 012, 017 and 018 added; (c) `assurance_major` and `assurance_minor` present in every new MSR-20 value. Evidence `sha256` recomputed 2026-09-26: 30 of 36 match; the 6 mismatches (INSP-003, INSP-004, INSP-009, INSP-017) are records edited after the append by their own closure passes, which the new section 11.1 evidence-preservation rule handles by a superseding record at commit (observation 5) |
| <a id="finding-5"></a>finding-5 (F-05) | assurance | Minor | SA-013-1, CK-REQ-G3 | 07 section 2.1.1 row "Other process documents (`docs/process/0N-*.md` except 03 and this plan) ...: No"; section 10.1 row b ("every `docs/process/0N-*.md`" as SWE-087 b software plans); section 1.3 row d | NPR 7150.2D 6.1 item d, the Software CM plan, is `docs/process/05-configuration-and-data-management.md` (07 section 1.3 row d), and section 10.1 row b treats it as a software plan, yet section 2.1.1 routes it to no assurance review; INSP-006 ran with `assurance_reviewer_agent: none`. SWEHB `swe-013` section 7.1 task 1 asks the assurance function to "confirm that all plans, including security plans, are in place and have expected content", and section 7.2 item 1 lists the "Software Configuration Management Plan" among the plans the assurance plan-review checklists cover. INSP-010 finding-10 raises the same routing gap for the make/buy record TS-002 (6.1 item t); this finding adds the CM plan and the SWE-013 basis. Fix: add the software CM plan and the 6.1 item t make/buy record to the "Software plans" row of section 2.1.1 (Yes, Yes, Yes), and have Claude dispatch the assurance review of 05 before the SRR declaration or record the owner's decision that 05 is covered by this record's SA-013-1 result | Verified | | Closed (iteration 2, verified 2026-09-26). 07 section 2.1.1 row "Software plans" names the software CM plan 05 (6.1 item d) and TS-002 (6.1 item t) with the SWEHB `swe-013` 7.1 task 1 and 7.2 item 1 basis (Yes, Yes, Yes); "Other process documents" now excludes 03, 05 and 07; new row routes trade studies and ADRs that constrain a safety-critical or mission-critical component (Yes, Yes, No); section 10.1 row b names 05 and TS-002. Dispatch of the 05 and TS-002 assurance reviews (or the owner's coverage decision) and the `ASSURANCE_WHOLE_PRODUCTS` tool constant are carried in 07 section 22 row "Assurance routing of 05 and TS-002", due before the SRR readiness declaration |
| <a id="finding-6"></a>finding-6 (F-06) | assurance | Minor | SA-013-2, CK-REQ-G8 | 07 section 15 table rows "5.17 items 1 to 3, 5.18 item 2.5", "5.17 item 13" and "5.17 item 14" | Checked against `docs/references/md/swehb/5-17-software-assurance-plan-minimum-content.md` lines 9 to 51. (a) 5.17 item 1 is "Introduction" (purpose, scope, overview), not an assurance activity; the row labels items 1 to 3 as activities and methods, and the introduction content (07 header and section 1.1) is not mapped. (b) Item 13 asks the plan to "identify all NPR 7150.2 requirements to be implemented and the associated NASA-STD-8739.8 tasking to be performed", satisfiable by the SA Tasking Checklist Tool of topic 8.15; the row points to `rmm.json` rows, which have no assurance tasking field, and to `assurance_tasks_applied`, which records tasks after each review rather than planning them. The consolidating checklist `peer-review-checklist-software-assurance.md` is due before PDR (08 section 3.5) but the row does not name it as the item 13 closure. (c) Item 14 says "at a minimum, collect and report on the software assurance metrics specified in NASA-STD-8739.8"; the row maps MSR-20 to MSR-23 and MSR-27 without stating the relief (the standard is not in the corpus; RMM SWE-022 T). SWEHB `swe-013` section 7.1 task 2 asks for an SA plan "following the content defined in NASA-HDBK-2203". Fix: split the row as item 1 (header and section 1.1) and items 2 and 3; name the SA checklist with its due gate as the item 13 closure (or add a SWE-to-task table); state the item 14 relief with the RMM SWE-022 reference | Verified | | Closed (iteration 2, verified 2026-09-26). 07 section 15 table: 5.17 item 1 (introduction) mapped alone to the header and section 1.1; items 2 and 3 separate; item 13 states "Partial at SRR" with closure by the SWE-to-task table of `peer-review-checklist-software-assurance.md` (SWEHB topic 8.15 form), due PDR; item 14 states the relief with the `rmm.json` SWE-022 T reference, mirrored in the section 22 SWE-022 row ("Section 15 (items 13 and 14 relief)") |
| <a id="finding-7"></a>finding-7 (F-07) | assurance | Minor | SA-220-1, CK-REQ-G1 | 07 section 14.1 paragraph after the table ("CC limit 15 with no waiver (SWE-220)"); section 14.3 ("no waiver is planned for a safety-critical component or unit"); `rmm.json` row SWE-220 ("any exceedance requires an owner waiver with rationale in the decision memo of the next review and is listed in the VDD") | NPR 7150.2D 3.7.5 [SWE-220] (`npr-7150-2d/03-chapter3.md` line 219): "Any exceedance shall be reviewed and waived with rationale by the project manager or technical approval authority." Section 14.1 reads as a prohibition of waivers for safety-critical code, section 14.3 as a plan without one, and the RMM row (FC) as a permitted owner waiver. The three texts disagree on what happens when a safety-critical function exceeds 15; section 8.2 (Complexity row) admits an owner waiver for any function. Fix: state once, in section 14.3, that no waiver is planned and that an exceedance in a safety-critical unit is reviewed and waived only by the owner with rationale in the decision memo, listed in the VDD (RMM SWE-220), and make section 14.1 point to it | Verified | | Closed (iteration 2, verified 2026-09-26). 07 section 14.3 is the single waiver rule and quotes NPR 7150.2D 3.7.5 verbatim (corpus `03-chapter3.md` line 219): no waiver planned; an exceedance in a safety-critical unit is reviewed by the independent and assurance reviewers and waived only by the owner with rationale as `W<n>` in the decision memo, CSA item 12 and the VDD; G5 fails until then. Section 14.1 points to it ("under the single waiver rule of section 14.3"); section 8.2 Complexity row ("without an owner waiver in the latest decision memo") and `rmm.json` SWE-220 agree |

## Readiness criteria (all true before the review starts)

| # | Criterion | Answer | Evidence |
|---|---|---|---|
| R1 | The product validates: `tools/validate_docs.py` exits 0 | No | Run 2026-09-25: exit 1, `FAIL docs/plan/measurements.json (schema: docs/plan/measurements.schema.json) - schema not found` (and `docs/design/allocation.json`, outside this product). Carried by INSP-010 finding-2 (Major), re-verified here; 07 section 22 row "Measurements schema and tool rows" schedules the schema before the SRR readiness declaration. Not duplicated as a finding in this record |
| R2 | `tools/traceability.py` reports no violation for the ids in the file | Yes | 07 defines no requirement or test case ids; `--report-only` run exit 0 with 2 violations, both on REQ-SYS-183 (outside this product) |
| R3 | The author's return states the self-check against sections A to G and the brief's acceptance criteria | No | No author return was supplied with this assignment; the 07 revision table row A.3 lists the changes, not a section G self-check. Recorded as a dispatch gap in the return (open question), as INSP-010 did |
| R4 | Every TBR has owner, plan, close_by; no TBD in the file | Yes | `grep -c -w TBD` on 07: 0. Every TBR named in 07 section 14 belongs to an L1 requirement (REQ-SYS-004, 009, 020, 048, 053, 054, 055, 097, 118, 131, 155, 162, 180, 181, 182 carry `tbr` objects; checked in `requirements.json`); `measurements.json` holds no TBR |
| R5 | For a CR: impact assessment attached | N/A | Not a CR |

## Plan review items (CK-REQ-G1 to G8 and A8; 08 section 3.1, 07 section 10.1 row b)

| Id | Answer | Evidence |
|---|---|---|
| CK-REQ-G1 | No | finding-2 (07 module rows versus 03 section 5 and the RMM SWE-134 row), finding-7 (SWE-220 waiver text versus the RMM row). Concurred with INSP-010 finding-6 (14.1 and section 22 statements that 03 is not yet re-transcribed; 03 header reads "sections 4.2, 4.3, 5 and 9 re-transcribed from `hazards.json` 0.3.0-pha" and its line 138 scheduler row carries a, b, c, e) |
| CK-REQ-G2 | Yes | Every section 3.4 phase, 8.4 gate and 10.2 element names its artifact and path (`docs/sprints/SW-NN-<module>.md`, `docs/reviews/<REVIEW>/checklists/<product-slug>.md`, `docs/plan/measurements.json`, `docs/vv/reports/TC-SW-COV-001-r<N>.md`); "as appropriate" and "should consider": 0 hits; the open items of section 22 name who changes what and by which gate |
| CK-REQ-G3 | No | Roles, independence and the owner's approval points are stated (sections 2.1, 1.3 owner action column, 3.1, 23), but the assurance routing is incomplete: finding-3 (separate assurance record not provided for), finding-5 (CM plan not routed) |
| CK-REQ-G4 | No | Concurred with INSP-010 finding-1, re-verified: `rmm.json` has SWE-045 and SWE-094 at `FC`, while 07 section 22 lists them among rows "dispositioned T or NA". The SWE-017, 022, 023, 143, 211 and 219 rows are `T` in `rmm.json` and are mirrored in section 22. Not duplicated here |
| CK-REQ-G5 | Yes | Section 16.2: six assets with surface, threat, 5x5 inputs and mitigation; section 16.4 names `TC-SW-BOOT-*`, `TC-SW-CFG-*`, `TC-SW-KEYER-*`, `TC-SW-DIAG-*` and gate G5; residual risks tagged `cyber` (RSK-015, RSK-022, RSK-060, RSK-061, RSK-062 per 06 SWE-154 row) |
| CK-REQ-G6 | No | Section 11.2 names source, threshold, collection point and rule for MSR-01 to MSR-28 and section 11.1 the storage and record format; the repository itself is not current for the SRR package (finding-4) and fails validation (INSP-010 finding-2) |
| CK-REQ-G7 | No | The SA-relevant tool claims are honest: section 8.1 states that no MC/DC instrumentation exists for Rust and that `--mcdc` has no effect; section 9.4 states that Emulation carries no timing credit; CS-03 and section 8.1 state that the nightly is non-credit. One claim is false: section 8.1 says the lock "still lacks the pinned nightly", while `tools/toolchain.lock.md` line 20 is the `nightly-2026-08-24 (date-pinned)` row with `rustc 1.100.0-nightly (fb6531d55 2026-08-23)` (read 2026-09-25). Concurred with INSP-010 finding-8; not duplicated |
| CK-REQ-G8 | Yes, with finding-6 | Pinned in the corpus: SWE-013 (`03-chapter3.md` line 31), SWE-022 (159), SWE-205 (183), SWE-023 (185), SWE-134 a to l (187 onward), SWE-219 (215, with the waiver note), SWE-220 (219), SWE-089 (`05-chapter5.md` line 73); 6.1 items a to y (`06-chapter6.md`); SWEHB 5.08 has 25 items (lines 30 to 62), 5.17 has 19 and 5.18 has 9 top-level items; SWEHB `7-02` section 1.2 criteria a to e at line 58; `swe-134` section 7.1 tasks 1 to 6; `swe-205` section 7.1 task 1. No NASA requirement is quoted inexactly; the 5.17 item mislabel is finding-6 |
| CK-REQ-A8 | Yes | Terminology matches Annex D and the L1 set: state names Boot, SafeState, Receive, TxPending, Transmit, Fault, ChargeInhibited (section 5 item 1, 14.2 row b), "straight key", "iambic paddle", `PA_EN`, `TX_KEY` used consistently |

## Software assurance tasks applied (SWEHB section 7.1 tabs)

| Id | SWEHB task | Answer | Evidence |
|---|---|---|---|
| SA-013-1 | `swe-013` 7.1 task 1: all plans, including security plans, are in place with expected content and tailoring for the class | No | In place (checked by path on 2026-09-25): 6.1 a and j, k, l, u, x in 07 (sections 20, 15, 14, 18, 17.1); b `docs/plan/schedule.md`; c `docs/plan/cost-estimate.md`; d `docs/process/05-configuration-and-data-management.md`; r `docs/risk/register.json`; t `docs/decisions/trade-studies/TS-002-firmware-runtime-make-buy.md` (INSP-013 APPROVED); security plan 07 section 16. Expected content per SWEHB 5.08: the 25 items map to named sections (07 section 1.4, each target read). Assurance coverage of the plans is incomplete: finding-5 (CM plan), finding-3 (record structure) |
| SA-013-2 | `swe-013` 7.1 task 2: SA plan following the HDBK-2203 content | No | 07 section 15 maps SWEHB 5.17 and 5.18; 5.18 items 1 to 9 map correctly to sections 14.1, 2.1, 2.2, 3, 23 and Annex D; the 5.17 map has the defects of finding-6 |
| SA-022-1 | `swe-022` 7.1 task 1: perform SA per the standard and the project's SA plan | Yes | This record is that performance for 07; the standard's relief is RMM SWE-022 `T` with rationale (standard not in the corpus), mirrored in 07 section 22 |
| SA-033-1 | `swe-033` 7.1 task 1: options for acquisition versus development evaluated | Yes | TS-002 sections 2 to 5 cover SWE-033 options a to f with alternatives A0 (rustos), A1 (`rp-hal` with `cortex-m-rt`), A2 (Embassy), A3 (RTIC) (TS-002 lines 275 to 290 name the evidence per task); independent review INSP-013 `APPROVED` |
| SA-033-3 | `swe-033` 7.1 task 3: risks of the acquisition versus development decision assessed | Yes | TS-002 risk table ("Aggregate risk per alternative (maximum score): A0 9, A1 12, A2 12, A3 12. No alternative carries a Red safety risk"); 07 section 21 rows RSK-013 and "Single maintainer of rustos" carry the chosen option's risks |
| SA-089-1 | `swe-089` 7.1 task 1: the project records peer review and inspection measurements | No | Per-record front matter carries the 01 section 13 measurements in all 16 SRR records (checked); the roll-up is stale and incomplete (finding-4) and keyed so that paired records collide (finding-3) |
| SA-134-1 | `swe-134` 7.1 task 1: requirements and design implement items a to l | No | The twelve shared rows of 14.2 each carry a provision, an L1 column, a reserved `REQ-SW-SAFE-NNN` and a verification cell; the numbers were checked against `requirements.json` (REQ-SYS-004 20 ms, 020 5.5 s, 048 2 ms, 052 500 ms, 053 5 s, 054 128 elements or 10 s, 055 7.5 to 13 s, 063, 064 1 W, 067 1 s, 074 30 mVrms, 075 60 dB within 2 ms, 097 3.20 V, 118 85 C and 100 ms, 120, 130 order, 131 2 s, 155 -20 C to +150 C, 162 5 ms, 180 180 s, 181 95 C, 182 10 kHz: all equal). The module rows are short of the governing allocation (finding-2) and row d rests on unisolated menu code (finding-1) |
| SA-134-4 | `swe-134` 7.1 task 4: partitioning or isolation; safety-critical elements and data logically isolated | No | finding-1. Positive evidence present: the frequency verification unit shares no code with the `SW-SYNTH` word path (14.1, 14.2 row i); the no-gap watchdog sits in `SW-SAFE` so a keyer fault cannot disable it (14.1); CS-38 keeps decisions host-compilable; criticality fixed per code unit (14.1 last paragraph). No general isolation rule for safety-critical state |
| SA-134-5 | `swe-134` 7.1 task 5: participate in reviews affecting safety-critical products | Yes | 07 sections 2.1.1 and 10.1 route every safety-critical and mission-critical requirement, design, code and test product to the assurance reviewer; section 15 adds NCR severity confirmation and sprint acceptance checks |
| SA-134-6 | `swe-134` 7.1 task 6: SWE-134 implementation consistent with the system hazard analysis | No | 14.1 recomputed from `hazards.json` 0.4.0-pha by the union rule: all 11 component rows equal in hazards and criteria (keyer HZ-001, 004, 010, a b c; TX sequencer eight hazards, a b c e; power HZ-002, 007, 011, c e; thermal HZ-003, b e; audio HZ-005, b c; safe-state manager nine hazards, a b c e; boot and configuration guard HZ-014, a c e; scheduler nine hazards, a b c e; frequency rows HZ-008, a c e). Module rows versus `swe134_items` unions: equal or larger for `SW-KEYER`, `SW-TXSEQ`, `SW-PWR`, `SW-AUDIO`, thermal unit, `SW-BOOT`, `SW-SYNTH` path. Short of the section 7 Components allocation for `cfg_guard` (finding-2) |
| SA-205-2 | `swe-205` 7.1 task 2: hazard reports identify the software components per the criteria | Yes | Each hazard of `hazards.json` 0.4.0-pha with a firmware role names `firmware_role.components` and criteria a to e; 07 section 14.1 names the module for each (see SA-134-6) |
| SA-205-3 | `swe-205` 7.1 task 3: hazard analyses identify the software components | Yes | `hazard-analysis.md` section 6.2 table equals 07 section 14.1 row for row; section 6.2 states 0.4.0-pha changes no `firmware_role` or `swe134_items`, confirmed by this reviewer from the 0.4.0 history notes of all hazards |
| SA-219-1 | `swe-219` (NPR note): 100 percent MC/DC, deviations waived by the TA, testing independent of the developer | Yes | 07 section 9.6: decision tables per safety-critical decision, independence pairs written by the test author (an invocation independent of the implementation author, section 3.4 phase 2), MSR-13 credit-bearing, MSR-14 non-credit cross-check; shortfall only by owner waiver `W<n>` (item 4); tailoring RMM SWE-219 `T` with residual RSK-010 |
| SA-220-1 | `swe-220`: complexity 15 or lower, exceedance reviewed and waived with rationale | No | CS-17, CS-38, G5 and MSR-17 enforce 15; the waiver text disagrees across 14.1, 14.3 and the RMM (finding-7) |

## Observations (not findings)

1. `hazards.json` version pointer. 07 sections 14.1, 14.2 and 22 cite `0.3.0-pha`; the file is `0.4.0-pha`. The 0.4.0 history note of every hazard states "no change to severity, likelihood, risk or status" and adds only L2 back-links and causes; `firmware_role` and `swe134_items` recomputed today equal 07 section 14.1, so the transcription rule of 14.1 (re-transcribe on any `firmware_role` or `swe134_items` change) was not triggered. Updating the pointer at the next edit is editorial.
2. Scheduler item set. The paragraph after the twelve-row table states the union rule and then exempts `SW-SCHED` ("named in `firmware_role.components` for its item j role only"), giving a, c, f, j, k, l instead of the a to l union of its nine hazards. The exemption is stated and justified, and the set is a superset of `hazard-analysis.md` section 7 (a, c, j, k); the software design at PDR should confirm that the scheduler has no transition logic of its own (item b) and no single event that can arm RF (item i).
3. HZ-008 type (ii) findings. 03 section 4.3 records that the safe-state manager and the scheduler meet criteria for HZ-008 (03 item X14, for the hazard analysis author). 07 follows `hazards.json` and does not list HZ-008 for them; the criteria unions do not change. No action on 07 until `hazards.json` changes.
4. Charter section 10 still names "mission-critical frequency control and configuration load"; 07 section 22 carries the change after package decision 9. Consistent with 07.

## Concurrence with INSP-010 (the paired peer review of the same blobs)

Read after this reviewer's checks. Re-verified and concurred: finding-1 (SWE-045 and SWE-094 are `FC` in `rmm.json`), finding-2 (`measurements.json` fails `validate_docs.py`, exit 1 reproduced), finding-6 (03 now re-transcribed; 07 statements stale), finding-8 (the lock has the dated nightly row), finding-10 (TS-002 assurance routing; extended by this record's finding-5 to the CM plan), finding-13 (row i L1 column lists REQ-SYS-181, which `hazard-analysis.md` section 7 row i does not list). Not re-verified: findings 3, 4, 5, 7, 9, 11, 12. No INSP-010 finding is disputed. This record's findings 1, 2, 3, 4, 6 and 7 are not in INSP-010.

## Tool runs (2026-09-25, this reviewer)

| Command | Exit | Result |
|---|---|---|
| `.venv/bin/python tools/validate_docs.py` | 1 | 33 passed, 2 failed: `docs/design/allocation.json` and `docs/plan/measurements.json` (schema not found); re-run after this record was written: exit 1 with the same two failures and `PASS docs/reviews/SRR/checklists/software-plan-07-software-assurance.md` (built-in peer-review record schema) |
| `.venv/bin/python tools/traceability.py --report-only` | 0 | 232 requirements, 167 test cases, 2 violations (REQ-SYS-183: `HAZARD_REQ_NOT_TESTED`, `REQ_UNVERIFIED`), 6 warnings (`SYS_UNALLOCATED`); none on this product |
| `.venv/bin/python tools/render_risk.py --check --gate SRR --hazards docs/safety/hazards.json` | 0 | register OK: 65 risks, 159 candidates, 0 warnings, hazard cross-check |
| `.venv/bin/python tools/render_rmm.py --check` | 0 | `rmm.md` is current |
| `.venv/bin/python tools/render_compliance.py --check` | 0 | validation passed, rendered file current |
| `.venv/bin/python -m unittest discover -s tools/tests` | 1 | 331 tests, 1 failure: `test_validate_docs.RepositoryTests.test_repository_exit_zero`, the same two schema-not-found failures |

## Participants

Author agent `author:software-plan` (absent). File reviewer `reviewer:software-plan` (paired record INSP-010, separate invocation). Software assurance reviewer `sa-reviewer:software-plan` (this record). Owner (Robin, SMA TA) for the disposition of the findings. Visual products: none produced or changed by this review.

## Measurements (SWE-089)

Product size: 07 revision A.3, 960 lines, 23 sections and annexes A to D; `measurements.json` 47 records. Items checked: 29 (R1 to R5, CK-REQ-G1 to G8, CK-REQ-A8, 15 SA items). Items answered No: 14 (front matter `items_no`). Items N/A: 1 (R5). Findings: 2 Major, 5 Minor, all from the assurance function, all Open; fixed 0, verified 0, deferred 0. Iteration 1. Effort: 44 agent turns, 65 minutes.

## Closure verification (iteration 2, 2026-09-26)

Same role (`sa-reviewer:software-plan`), new invocation; the product was not edited by this reviewer. Input: the author's fix list for INSP-018 finding-1 to finding-7 (no dispute against this record; the one dispute the author lists, part of INSP-010 finding-11, belongs to the INSP-010 reviewer). Each finding was checked against 07 blob `eba15bc` (1007 lines), `measurements.json` blob `5e2d175`, 03 blob `fdc8d07` and `rmm.json` blob `78c3b23`. Search-first: `mcp__claude-context__search_code` on `/Users/robinonsay/rust/cwht` ran before any `grep` (query: 07 section 14.2 row d receiving-side validation of two operator events); `grep` was then used only to pin the SWEHB `swe-134` task 4 a and b lines (155, 157) and the 07 Complexity rows; the SWE-134 allocation comparison and the evidence-hash recomputation were read-only Python over the files.

| Finding | Iteration 1 severity | Result | Evidence (short; full text in the Disposition column) |
|---|---|---|---|
| finding-1 | Major | Closed | 07 sections 5 item 5, 7.8 CS-39, 14.1 Proposed menu override row and Owner decision paragraph, 14.2 row d and menu module row |
| finding-2 | Major | Closed | Every 07 section 14.2 module row a superset of 03 section 5; one governing source stated in 07, 03 and `rmm.json` SWE-134 |
| finding-3 | Minor | Closed | 07 sections 2.1, 2.1.1, 10.2, 10.3, 11.1, 15; tool and front matter follow-ups in section 22 |
| finding-4 | Minor | Closed | 34 new MSR-20 and MSR-21 records keyed on INSP id; INSP-007 and INSP-008 corrected; assurance keys present |
| finding-5 | Minor | Closed | 07 section 2.1.1 rows, section 10.1 row b; dispatch in section 22 |
| finding-6 | Minor | Closed | 07 section 15 rows 5.17 item 1, items 2 and 3, 13, 14 |
| finding-7 | Minor | Closed | 07 section 14.3 single rule quoting 3.7.5; 14.1, 8.2 and `rmm.json` SWE-220 agree |

Assurance task results changed by the fixes: SA-134-1 Yes (module rows cover the governing allocation; row d validated on the receiving side); SA-134-4 Yes (section 5 item 5 and CS-39 state logical isolation of safety-critical state and data; the MPU question is routed to the PDR architecture ADR); SA-134-6 Yes (`cfg_guard` now carries e); SA-013-1 Yes for 07 (05 and TS-002 routed; their assurance records are a dispatch item, below); SA-013-2 Yes (item 13 is Partial at SRR with a named closure due PDR, acceptable for a preliminary plan at SRR per 01 section 4.3 row 23); SA-089-1 Yes; SA-220-1 Yes. CK-REQ-G1, G3 and G6 Yes for this record's findings (G6 still depends on the schema, R1). The iteration 1 `items_no` list is kept in the front matter as the SWE-089 measurement of iteration 1.

Observations added at iteration 2 (not findings):

5. Evidence hashes. Four records rolled up on 2026-09-26 (INSP-003, INSP-004, INSP-009, INSP-017; 6 of 36 evidence entries) changed after the append; this record changes too with this edit. Under the new 07 section 11.1 evidence-preservation rule the software lead supersedes them at the commit that first adds `measurements.json`. INSP-015 (`tool-validation-tv-001-to-tv-010.md`, dated 2026-09-26) has no MSR-20 or MSR-21 record yet; 07 section 10.3 requires it at the SRR package build.
6. Key-input mode change in two classes. 07 section 14.1 lists "Straight-on-tip and every other key-input mode change (REQ-SYS-056, REQ-SYS-163)" in the Proposed safety-critical menu override row, and the mission-critical paragraph lists key-input mode selection (REQ-SYS-056, REQ-SYS-163) in the selection path. 03 section 4.3 has the same wording, and both defer the code-unit boundary to `docs/design/software-design.md` at PDR, so this is consistent; the PDR design review should confirm that the override confirmation units are the safety-critical ones and the presentation units the mission-critical ones.

Items this closure depends on, outside this record and carried in 07 section 22 (no action by this reviewer): the measurements schema and its known-answer fixture (readiness R1; INSP-010 finding-2); the `paired_record` field in 01 section 13 and `tools/validate_docs.py`, and INSP-010 naming this record in `assurance_reviewer_agent` and `paired_record` with `assurance_verdict` copied (INSP-010 still reads "not yet dispatched" and `pending`); the paired assurance reviews of 05 and TS-002; the owner's X20 ruling on the menu override command path at the SRR decision memo.

### Tool runs (2026-09-26, this reviewer)

| Command | Exit | Result |
|---|---|---|
| `.venv/bin/python tools/validate_docs.py` | 1 | 35 passed, 2 failed (`docs/design/allocation.json`, `docs/plan/measurements.json`: schema not found); this record PASS after the iteration 2 edit, with `paired_record` accepted |
| `.venv/bin/python tools/traceability.py --report-only` | 0 | |
| `.venv/bin/python tools/render_risk.py --check --gate SRR --hazards docs/safety/hazards.json` | 0 | `register.md` current |
| `.venv/bin/python tools/render_rmm.py --check` | 0 | `rmm.md` current |
| `.venv/bin/python tools/render_compliance.py --check` | 0 | validation passed, rendered file current |
| `.venv/bin/python -m unittest discover -s tools/tests` | 1 | 335 tests, 1 failure (`test_repository_exit_zero`, same cause as the validate_docs failures) |

### Closure block

- Findings: 7 (2 Major, 5 Minor), all from the assurance function.
- Closed and verified: 7. Still open: 0. Disputed: 0. Deferred: 0.
- `reviewer_verdict` and `assurance_verdict` at iteration 2: APPROVED. Record `verdict` stays NEEDS CHANGES under the 07 section 10.2 pairing rule until INSP-010 is APPROVED, and readiness R1 stays No until the measurements schema is committed (INSP-010 finding-2); both are outside this record.
- `record_status` stays Open (set to Closed by the software lead); `date_closed` null.
- Iteration 2 effort: 28 agent turns, 35 minutes (front matter totals 72 turns, 100 minutes).

## Verdict

```
VERDICT: NEEDS CHANGES
FINDINGS:
- [Major] finding-1 SA-134-4, SA-134-1: 07 14.1 Neither paragraph and 14.2 row d rest the SWE-134 d overrides on non-safety menu code with no isolation provision; add receiving-side validation or a partitioning rule, or class the override path safety-critical.
- [Major] finding-2 SA-134-1, SA-134-6: 07 14.2 module rows allocate fewer SWE-134 items than 03 section 5 (cfg_guard e; SW-CFG b, e, g; SW-DISPLAY a, b; SW-SYNTH mission a, b, e, g); align and name one governing source with 03 and the RMM.
- [Minor] finding-3 CK-REQ-G3: separate assurance records are not provided for in 07 2.1, 10.2, 15; MSR-20/21 scope collides for paired records.
- [Minor] finding-4 SA-089-1: measurements.json roll-up stale for INSP-007 and INSP-008 and missing six records.
- [Minor] finding-5 SA-013-1: CM plan (6.1 d) not routed to assurance review in 2.1.1.
- [Minor] finding-6 SA-013-2: 5.17 items 1, 13 and 14 mapped incorrectly or without closure.
- [Minor] finding-7 SA-220-1: SWE-220 waiver text disagrees across 14.1, 14.3 and the RMM row.
ITEMS N/A: R5 (not a CR); CK-REQ sections A1 to A7, B to F (product is a plan)
MEASUREMENTS: size=960 lines + 47 records; items=29; no=14; turns=44; minutes=65; major=2; minor=5
```

Iteration 2 (2026-09-26):

```
VERDICT: APPROVED (assurance review); record verdict held at NEEDS CHANGES pending INSP-010 and readiness R1
FINDINGS: finding-1 to finding-7 Closed and Verified; 0 open
MEASUREMENTS: iteration=2; closed=7; open=0; turns=28; minutes=35
```
