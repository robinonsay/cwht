---
# Peer-review record front matter (charter section 5; docs/process/01-lifecycle-and-reviews.md section 13
# is the single field list; docs/process/08-agent-briefing.md section 3.2; template
# docs/templates/peer-review-checklist-classification.md revision A).
# This is the separate software assurance record of the classification concurrence (SWE-020 guidance
# paragraph, SWE-176). The paired peer review of the same product is INSP-009,
# docs/reviews/SRR/checklists/classification-03-software-classification-and-rmm.md (reviewer:classification).
id: INSP-017
checklist: peer-review-checklist-classification
checklist_revision: A
checklist_file: docs/reviews/SRR/checklists/classification-03-software-classification-and-rmm-software-assurance.md
product: docs/process/03-software-classification-and-rmm.md
# product_commit: git blob (git hash-object) of the reviewed working-tree 03 (last commit 4e3f891; not yet
# committed, SRR package section 2 item H17). All product blobs are in product_files.
# Iteration 2 (2026-09-26) verifies the fourth revision: 03 blob fdc8d07..., rmm.json 78c3b23..., rmm.md fe374a4...
# Iteration 1 reviewed 03@c87abe5a09985c3f0a3adb74e17386cc61d731b4, rmm.json@3645b4f6..., rmm.md@07055619...
product_commit: "fdc8d0764e7ee513a05215de7dfb580687b721d4"
product_files: ["docs/process/03-software-classification-and-rmm.md@fdc8d0764e7ee513a05215de7dfb580687b721d4", "docs/process/rmm.json@78c3b2364473e0676f8b5405ecd9f8536715bea1", "docs/process/rmm.md@fe374a48e3eb9b9d878e07ff3f0e7fcf9c52ad09"]
# inputs read (not reviewed): hazards.json 0.4.0-pha (SHA-256 f9325b367a2553cc0152dafbb3110535c2a4f2159f1f684ee014936b451d59d1,
# unchanged since iteration 1) and 07 and hazard-analysis.md at the blobs below
input_files: ["docs/safety/hazards.json@89d0cbc323b2775d326cf7cd42bc637818ec96eb", "docs/process/07-software-engineering-plan.md@012ed916d7e39ca505d2bd027c1f63b685814084", "docs/safety/hazard-analysis.md@c20281a7d6cf64f38d678537de895ca7908fc11f"]
product_size: 5 inventory items and 8 per-item classification rows; 10 component rows determined (2 proposed) plus 5 verification-software items; SWE-134 allocation of 12 items; RMM of 100 rows
sprint: SRR-prep
author_agent: "author:classification-rmm (Claude lead SE and software lead, 03 and RMM author; third revision 2026-09-25, H13 re-transcription, uncommitted)"
reviewer_agent: "sa-reviewer:classification"
criticality: safety-critical
assurance_required: true
# assurance_reviewer_agent: this record IS the software assurance review; its reviewer is sa-reviewer:classification.
# The field is distinct from the author and from the file reviewer of the paired record INSP-009
# (reviewer:classification). validate_docs.py compares the field string with reviewer_agent of this file,
# so the annotation below is what separates them; see the Participants section and the returned charter issue.
assurance_reviewer_agent: "sa-reviewer:classification (software assurance function; paired peer review INSP-009 by reviewer:classification)"
iteration: 2
readiness_met: false
reviewer_verdict: NEEDS CHANGES
assurance_verdict: NEEDS CHANGES
verdict: NEEDS CHANGES
findings_major: 1
findings_minor: 6
findings_open: 2
findings_fixed: 5
findings_verified: 5
findings_deferred: 0
assurance_findings_major: 1
assurance_findings_minor: 6
assurance_tasks_applied: [swe-020 7.1 task 1, swe-125 7.1 task 1, swe-125 7.1 task 2, swe-139 7.1 task 1, swe-176 7.1 task 1, swe-205 7.1 task 1, swe-205 7.1 task 2, swe-205 7.1 task 3, swe-205 7.1 task 4, swe-134 7.1 task 4, swe-134 7.1 task 6]
deferred_rids: []
items_no: [R1, R3, CL-2, CL-3, CL-4, CL-5, CL-9, CK-REQ-G1, SA-205-1, SA-134-4, SA-176-1]
effort_turns: 62
effort_minutes: 80
record_status: Open
date: 2026-09-26
date_closed: null
---

# Peer review record INSP-017: software assurance review of the classification concurrence (03, rmm.json, rmm.md)

**Product.** `docs/process/03-software-classification-and-rmm.md` (third revision, 2026-09-25, working tree, blob `c87abe5a09985c3f0a3adb74e17386cc61d731b4`, last commit `4e3f891`), with `docs/process/rmm.json` (blob `3645b4f682cba38373f0fca55beb190ded36c032`) and the rendered `docs/process/rmm.md` (blob `070556193080b70d5fdf388550523bade3357007`). Same blobs as INSP-009 reviewed, so both records address one product state. **Source of record read:** `docs/safety/hazards.json`, now version `0.4.0-pha` (blob `89d0cbc323b2775d326cf7cd42bc637818ec96eb`, SHA-256 `f9325b36...51d59d1`, file time 23:40 on 2026-09-25, edited after the 03 re-transcription and after INSP-009). **Authoritative component list read:** 07 section 14.1 (blob `1b8864b0121f3f8547e1b194d63b41752ab946b9`).

**Checklist.** `docs/templates/peer-review-checklist-classification.md` revision A (R1 to R3, CL-1 to CL-9). Added for this assurance review: the plan items CK-REQ-G1 to G8 of `docs/templates/peer-review-checklist-requirements.md` section G (08 section 3.1, "plans and process documents"; 07 section 2.1.1 row "Software plans" marks 03 Yes for the assurance review), and the SWEHB section 7.1 software assurance tasks of SWE-020, SWE-125, SWE-139, SWE-176, SWE-205 and SWE-134 (07 section 15: until `peer-review-checklist-software-assurance.md` exists, the assurance reviewer applies the SWEHB section 7 tabs directly). **SRR context:** `docs/reviews/SRR/package.md` section 2 items H1 (classification concurrence with a software assurance reviewer), H13 (re-transcription) and H16 (AL-02-19), section 6.9 table (SWE-020, SWE-176 "SA reviewer record: none").

**Search-first compliance.** `mcp__claude-context__search_code` on `/Users/robinonsay/rust/cwht` ran before any manual search (queries: classification concurrence and SWE-020, SWE-176, SWE-205 records; SWE-020 software assurance tasking). `grep -n` was used afterwards only to pin lines the hits pointed at; files at known paths were read directly.

**Independence.** This reviewer did not author 03, `rmm.json`, `rmm.md`, `hazards.json`, `hazard-analysis.md` or 07, and is a different invocation from the INSP-009 reviewer. The product was not edited. INSP-009 was read after this reviewer's own checks of CL-1 to CL-9 were done; section "Concurrence with INSP-009" records the result of re-checking its findings.

## Findings

Ids follow the `finding-<n>` anchor rule of 01 section 13 (the assignment's F-nn label in brackets). Severity per the checklist: Major when a dissent changes a class, the safety-critical set or the mission-critical set; Minor otherwise. The disposition column is left for the owner (01 section 10.1).

| Finding | Origin | Severity | Item | Location | Description | State | Deferred to | Disposition |
|---|---|---|---|---|---|---|---|---|
| <a id="finding-1"></a>finding-1 (F-01) | assurance | Major | CL-5, CL-9, SA-134-4, SA-205-1 | 03 section 4.3 "Not safety-critical and not mission-critical" paragraph (menu handling); section 5 row d; section 9 row "Safety-critical software"; mirrored in 07 section 14.1 "Neither" paragraph and `hazard-analysis.md` section 6.2 line "Not safety-critical and not mission-critical" | Menu handling is placed outside both sets, yet it is the path that issues every SWE-134 d operator override of a hazard control: selecting 5 W (HZ-001 K1, HZ-006 K1, REQ-SYS-063), raising the tune carrier above 0.5 W (REQ-SYS-019), unlocking the audio cap (HZ-005 K2, REQ-SYS-074, 169, 170: "menu action plus confirmation"), releasing the guest lock (HZ-006 K3, REQ-SYS-066), selecting Straight-on-tip (HZ-004 K2, REQ-SYS-056: "only by an explicit operator menu selection") and entering the bench test mode (HZ-004 K13, HZ-005 K9, REQ-SYS-179). 07 section 14.2 row d and `hazard-analysis.md` section 7 row d state that each needs "a menu action ... followed by a separate confirmation". A menu fault that emits the action and the confirmation from one gesture (encoder bounce, a stale event queue, a corrupted menu state) defeats SWE-134 d and contributes to HZ-001, HZ-004, HZ-005 and HZ-006 by incorrect action: criterion a of NASA-STD-8739.8B para 3.2 (SWEHB `7-02` line 58 onward) and the "action, inaction, or incorrect action" of SWEHB `swe-205` section 7.1 task 1; the SWE-134 note also names "software used to command hazardous operations". Section 5 row d allocates d only to the receiving components (PA enable and TX sequencer, keyer, audio limiter), and neither 03, 07 nor the hazard analysis states that those components enforce the two independent actions on their own inputs, treating menu output as untrusted; SWEHB `swe-134` section 7.1 task 4 b asks the assurance function to confirm that the design "logically isolates the safety-critical design elements and data from those that are non-safety-critical", and no such isolation is stated. As written, a component that meets criterion a for four hazards is excluded, which changes the safety-critical set. Fix, one of: (1) record a type (ii) finding (03 section 4.1 step 3) that the override command path of menu handling (generation of the action and confirmation events and the encoder and button input it reads for them) is safety-critical for HZ-001, HZ-004, HZ-005 and HZ-006 with criterion a, carry it into sections 4.3, 5 row d and 9, 6.5 (for `hazards.json` and 07 section 14.1) and the SWE-219 and SWE-220 scope of section 4.4; or (2) state in section 4.3 the partitioning constraint that makes the menu non-safety-critical: each receiving safety-critical component accepts an override only from two distinct operator events that it validates itself (distinct input sources or a minimum interval, rejecting both from one input sample), so that no menu fault alone can produce an override; flow it into section 5 row d, 07 section 14.2 row d and `REQ-SW-SAFE-004`, and add a HostUnit case that injects a faulty menu emitting both events. The owner rules as SMA TA (package decision 9 context). | Closed |  | Closed (iteration 2, verified 2026-09-26). Fix option (1) applied: 03 section 4.3 carries a **proposed** row "Menu override command path" (criterion a; HZ-001, HZ-004, HZ-005, HZ-006; the action and confirmation generation with the button, held-combination and encoder-push input) and the type (ii) finding paragraph citing SWEHB `swe-134` 7.1 task 4 b (verified: `swe-134` page line 157 "logically isolates the safety-critical") and `swe-205` 7.1 task 1; section 4.4 puts the path in the SWE-219 and SWE-220 scope; section 5 rows a, d, e, g and i allocate it; section 9 rows "Safety-critical software" and "Menu handling" and the header name it; section 6.5 carries X16 (`hazards.json`, `hazard-analysis.md` sections 6.2 and 7 row d), X1 (x) and (xi) (07 section 14.1 and 14.2 row d) and X20 (package decision for the owner as SMA TA); the partitioning alternative (option 2) is stated with its HostUnit faulty-menu injection case and the change set it triggers. `rmm.json` SWE-023, SWE-134 and SWE-205 name the path and X20 (script check). The safety-critical set now includes the path pending the owner ruling, which is the correct state for a proposal; X1, X16 and X20 remain tracked in 03 section 6.5 and 03 is not signed while X1 or X20 is unresolved (03 section 6.5 lead-in) |
| <a id="finding-2"></a>finding-2 (F-02) | assurance | Minor | R3, CL-4, CL-8, SA-205-4 | 03 header paragraph; section 4.2 heading, first paragraph (SHA-256 `e008671c...e3be37`) and "Firmware controls" column; section 4.3 last paragraph ("at 0.3.0-pha carries the `REQ-SYS-*` constraint-level controls; the requirements author adds the `REQ-SW-<SUB>-*` controls before PDR"); section 6.1 validation-commands note; section 9 row "Safety-critical software"; `rmm.json` rows SWE-020, SWE-023, SWE-134 and SWE-205 ("PHA 0.3.0-pha"; SWE-205: "gain the REQ-SW-* controls before PDR") | `hazards.json` is now `0.4.0-pha` (SHA-256 `f9325b36...51d59d1`), not the `0.3.0-pha` file whose hash 03 states, and 03 section 6.5 lists no difference. Checked field by field on 2026-09-25: `firmware_role.criteria`, `safety_critical`, `components` and `swe134_items` of all 15 hazards are unchanged, so the determination of sections 4.3 and 5 still holds (the criteria unions and the section 5 hazard lists recomputed from 0.4.0-pha equal 03 for all 9 component rows and all 12 SWE-134 items). What changed: the `control_req_ids` of 13 software controls now also name the 23 `REQ-SW-KEYER` and 17 `REQ-TX` back-links (for example HZ-001 K1 adds REQ-TX-004, HZ-004 K1 adds REQ-SW-KEYER-022 and 023, HZ-008 K4 adds REQ-TX-002, 003, 005, 006, HZ-008 K7 adds REQ-TX-003 and 013), which the section 4.2 column does not carry; the L2 links 03 section 4.3 and the SWE-205 row defer to PDR exist now (`hazard-analysis.md` section 9 item 1 withdraws that deferral); HZ-007 gains Software causes C9 and C10 and HZ-010 C6 and C7 (see finding-3). Also stale: section 6.1 attributes the `validate_docs.py` exit 1 to `docs/design/allocation.json` alone; the run of 2026-09-25 also fails `docs/plan/measurements.json` (schema not found). 03 section 6.4 item 9 requires re-transcription only when `firmware_role` or `swe134_items` change, so the determination is not wrong; the version pointer, hash, control column and the "before PDR" statements are. Fix: re-transcribe the section 4.2 control column from 0.4.0-pha, update the version and hash in the header, section 4.2, section 9 and the four RMM rows, correct the section 4.3 and SWE-205 statements on `REQ-SW-*` controls, and update the section 6.1 note; or record the 0.4.0-pha differences as an open item of section 6.5. | Closed |  | Closed (iteration 2, verified 2026-09-26). Header, section 4.2 heading and first paragraph cite 0.4.0-pha with SHA-256 `f9325b36...51d59d1` (equal to `shasum -a 256` of the file today); a script compared every Software control of all 15 hazards with the section 4.2 control column: every `K<n>` present with its `control_req_ids` set equal, and every `firmware_role.components` string present, 0 differences. The counts "15 distinct `REQ-SW-KEYER-*` and 14 distinct `REQ-TX-*`" match the file. Section 4.2 lists the 0.3.0-pha to 0.4.0-pha differences; section 4.3 last paragraphs and `rmm.json` SWE-205 now state that the L2 links of existing files are present and only modules without a requirement file wait for PDR; section 6.1 names both `allocation.json` and `measurements.json`; `rmm.json` SWE-020, SWE-023, SWE-134 and SWE-205 cite 0.4.0-pha. Residual count error in the new difference list is finding-7 |
| <a id="finding-3"></a>finding-3 (F-03) | assurance | Minor | CL-3, CL-5, SA-205-1 | `docs/safety/hazards.json` `firmware_role.criteria` of HZ-001, HZ-002, HZ-003, HZ-005, HZ-006, HZ-007, HZ-010, HZ-011, HZ-012; transcribed in 03 section 4.2 and summed in the section 4.3 criteria cells of battery and charging supervision (c, e), thermal protection (b, e) and audio limiter (b, c) | Each of these nine hazards carries at least one cause of class `Software` in `hazards.json` 0.4.0-pha, yet none records criterion a ("causes or contributes to a system hazardous condition or event"): HZ-001 C3 (power step reverts to 5 W, tune carrier does not time out), HZ-002 C4 (firmware writes a wrong charge profile or ignores a charger fault flag), HZ-003 C4 (fold-back does not act, stalled monitor task), HZ-005 C2 (full-scale PWM pattern, limiter bypassed), HZ-006 C3, HZ-007 C9 and C10 (labelled "inaction" and "incorrect action" in the file), HZ-010 C6 and C7 (labelled "incorrect action"), HZ-011 C2 (fails to disarm transmit on VBUS), HZ-012 C4. Only HZ-004, HZ-008 and HZ-014 carry a. SWEHB `swe-205` section 7.1 task 1 asks the assurance function to confirm that "all known software contributions or events where software, either by its action, inaction, or incorrect action, leads to a hazard" are in the hazard reports; the causes are there, the criterion is not. Adding a changes the unions of battery and charging supervision to a, c, e, of thermal protection to a, b, e and of the audio limiter to a, b, c; no component enters or leaves the safety-critical set, so the severity is Minor. This extends INSP-009 finding-5 (which names HZ-001, HZ-003, HZ-006 and HZ-012) with HZ-002, HZ-005, HZ-007, HZ-010 and HZ-011 and with the union effect; 0.4.0-pha added the HZ-007 and HZ-010 Software causes while stating that it "changes no firmware-role criterion" (`hazard-analysis.md` line 116). Fix (hazard analysis author, cross item): add criterion a to each hazard with a Software-class cause, or state in its `firmware_role.statement` why that cause is not a firmware contribution; 03 then re-transcribes sections 4.2 and 4.3 (section 6.4 item 9). | Open | X15 (hazard analysis author, before the SRR readiness declaration) | Open. The fix is in `hazards.json`, which is unchanged (same SHA-256 as iteration 1): a script check on 2026-09-26 finds Software-class causes without criterion a on HZ-001, 002, 003, 005, 006, 007, 010, 011 and 012 as before. 03 now does its part: section 4.3 paragraph "Criterion a on hazards with Software-class causes" states this record reads a as met, the resulting unions and that no set changes, and section 6.5 item X15 assigns the change to the hazard analysis author with re-transcription to follow. Closes when `hazards.json` adds a or states the per-cause reason and 03 re-transcribes |
| <a id="finding-4"></a>finding-4 (F-04) | assurance | Minor | CL-1, CL-2, SA-020-1 | 03 section 3.1 per-item table row `cwht-fw` ("D; Lowest class permitted for safety-critical software (App. D Class E a.3, c.8)") and the factor table row "Criticality to NASA programs" | The row places the firmware in Class D by excluding Class E only; it cites no Class D definition (a.1 to a.3) that embedded operational firmware meets (a.1 lists ground software and tools, a.2 airborne vehicle software, a.3 facility research software) and does not address Class D exclusion c.1, "Software that can impact primary or secondary mission objectives" (`10-appendixd.md` Class D c.1). 03 section 4.1 step 4 itself defines a primary mission objective (a CW contact in 144.000 to 148.000 MHz with either key type) and designates firmware components mission-critical because they can cause or contribute to losing it, so c.1 reads as applying unless 03 states the reading under which it does not (for example that "mission objectives" in c.1 means NASA program objectives, factor (2) of D.1 scored None). By D.2 an unresolved exclusion points above Class D, where no Class C definition fits either. The elected Class A applicability is unaffected, which is why this is Minor; the letter assessment is what SWEHB `swe-020` section 7.2.1 asks the assurance function to see justified ("Justification for the classifications assigned by software engineering"). Fix: state which App. D text places the firmware in Class D by analogy and how exclusion c.1 is read, or record the letter result as "no class fits by the letter; Class D is the floor for safety-critical software; Class A elected". | Closed |  | Closed (iteration 2, verified 2026-09-26). 03 section 3.1 `cwht-fw` row addresses Class D a.1 to a.3, reads exclusion c.1 strictly and records "no class fits by the letter; Class D is the floor; Class A elected", with Class E a.3, c.4 and c.8; the section 1 inventory cell, the conclusion paragraph, section 9 and `rmm.json` `meta.software_class_app_d_assessment` carry the same result (script check). This is the result the finding proposed |
| <a id="finding-5"></a>finding-5 (F-05) | assurance | Minor | CL-2, SA-139-1 | 03 section 3.1 per-item table row `tools/refs/*.py` (Class E, "c.5 does not apply because ... every use of it in a controlled product is cross-checked") | The row disposes of Class E exclusion c.5 but not c.6 ("Software used in technical decisions concerning operational systems, or systems being developed for operation"). The converter output is the input from which `tools/render_rmm.py` derives the RMM row set, `npr_section`, `authority_npr` and the verbatim `requirement_text` of all 100 rows (03 section 6.1: "parsed from `docs/references/md/npr-7150-2d/09-appendixc.md` (not hand-copied)"), which is the SWE-139 applicability decision for the project. The two-source cross-check (Table 2 against the chapter 3 to 5 tags) catches a dropped or extra SWE row but both sources come from the same converter run over the same NODIS document, and `requirement_text` is compared with the Table 2 cells only, so a converter fault that alters statement text is not detected by it. Fix: address c.6 in the row, and state the residual common-mode limit with its mitigation (for example the re-reading at the cited line under charter section 11 rule 1, or an owner spot check of N rows against the NODIS source recorded in the tool's TV record), or classify the converters Class D like the other evidence-producing tools. | Closed |  | Closed (iteration 2, verified 2026-09-26). 03 section 3.1 splits the converters: `nodis_to_md.py` and `npr7150_to_md.py` are Class D (a.1(b); Class E excluded by c.6 and c.5) and elected Class A, with the common-mode limit of the two-source cross-check stated; the SE HB and SWEHB converters stay Class E with c.5 and c.6 each addressed. Section 6.1.1 and item X17 carry the category B `TV-NNN` record with an independent-path known-answer test; sections 1, 3.2 and 9 and `rmm.json` meta name the split. The finding offered Class D as a sufficient fix; X17 is the tool-validation follow-up and 03 is not signed while it is unresolved |
| <a id="finding-6"></a>finding-6 (F-06) | assurance | Minor | SA-176-1, CK-REQ-G1 | 03 section 3.3 ("records the result as the single peer-review record `...classification-03-software-classification-and-rmm.md`"; "the record also names a software assurance reviewer"); section 9 row "Per-item classification" and the signature line "independent concurrence INSP-<NNN>"; `rmm.json` rows SWE-020 and SWE-176; `docs/templates/peer-review-checklist-classification.md` Record section | The independent classification assessment now consists of two records: the peer review INSP-009 and this software assurance record INSP-017 (the assignment's separate software assurance reviewer record; SRR package section 6.9 column "SA reviewer record"). 03, the RMM rows and the template name one path and one INSP id, and INSP-009's front matter names the assurance reviewer "assurance:classification (separate invocation, not yet run)" rather than this invocation. SWE-176 requires "the results of each software independent classification assessments" to be retained (`03-chapter3.md` line 155), and SWEHB `swe-176` section 7.1 task 1 asks the assurance function to confirm the records are maintained; as written, the retention trace and the SRR memo signature line would cite INSP-009 only. Fix: name both records (paths and INSP ids) in 03 sections 3.3 and 9, the signature line and the SWE-020 and SWE-176 rows, or have the lead SE merge the assurance result into INSP-009 and withdraw this file by a recorded decision; update INSP-009 `assurance_reviewer_agent` and `assurance_verdict` to point here. | Closed |  | Closed for the product (iteration 2, verified 2026-09-26). 03 section 3.3 has a two-row table naming INSP-009 and INSP-017 with paths, reviewers and scope; section 9 row "Per-item classification", the header, the signature line ("independent concurrence INSP-009 (peer review) and INSP-017 (software assurance)") and `rmm.json` SWE-020, SWE-176 and SWE-205 name both. The template Record section and the INSP-009 front matter (`assurance_reviewer_agent` still "assurance:classification (separate invocation, not yet run)", `assurance_verdict` pending, checked 2026-09-26) lie outside the author's files and are item X19 of 03 section 6.5 (lead SE and INSP-009 reviewer) |
| <a id="finding-7"></a>finding-7 (F-07, iteration 2) | assurance | Minor | CL-4 | 03 section 4.2, "Changes from 0.3.0-pha to 0.4.0-pha" item 2 | The item says the `control_req_ids` of "13 Software controls" gain L2 back-links, but the list that follows names 19 (HZ-001 K1, K3; HZ-003 K3; HZ-004 K1, K2, K3, K7, K8, K9, K13; HZ-005 K4; HZ-006 K1, K3, K6; HZ-008 K4, K7; HZ-010 K3; HZ-012 K2; HZ-014 K6), and a script count of `hazards.json` 0.4.0-pha gives 19 Software controls whose `control_req_ids` hold a `REQ-SW-KEYER-*` or `REQ-TX-*` id. The number came from this record's own finding-2 text (iteration 1), which was wrong; the enumerated list and the control column are right, so no determination is affected. Fix: change "13" to "19" | Open | | Open (raised at iteration 2); editorial-scale correction by the 03 author |

## Readiness criteria (all true before the review starts)

| # | Criterion | Result | Evidence |
|---|---|---|---|
| R1 | `tools/validate_docs.py` exits 0 (includes `hazards.json` and `rmm.json`) | No (outside the product) | Run 2026-09-25 23:4x: exit 1, "validate_docs: 31 passed, 2 failed, 33 checked"; `docs/process/rmm.json` and `docs/safety/hazards.json` PASS; failures `docs/design/allocation.json` and `docs/plan/measurements.json`, both "schema not found". Recorded as `readiness_met: false`; the review proceeded as assigned. |
| R2 | `tools/render_rmm.py --check` exits 0 | Yes | Exit 0: "rmm.json OK: 100 rows; FC=75, T=17, NA=8; In place=37"; "rmm.md is current". `tools/tests/test_render_rmm.py`: 20 tests OK. |
| R3 | The `hazards.json` version transcribed is stated, and every difference from the current file is an open item of section 6.5 | No at iteration 1; Yes at iteration 2 (03 section 4.2 states 0.4.0-pha and its hash and lists the differences) | 03 states 0.3.0-pha with SHA-256 `e008671c...`; the file is 0.4.0-pha with SHA-256 `f9325b36...`; the differences are not in section 6.5 (finding-2). |

## Checks CL-1 to CL-9 (copied from 03 section 3.3)

| # | Answer | Evidence |
|---|---|---|
| CL-1 | Concur, with finding-4 | Factor scores against `10-appendixd.md` D.1 (line 9) and the class definitions: usage, NASA criticality and Agency investment None; human dependence Low with the section 4 hazards; complexity low to moderate. The factor table is consistent with D.1. The firmware row's Class D placement is justified only by the Class E exclusions (finding-4). |
| CL-2 | Per item: `cwht-fw` Dissent (finding-4); firmware test tooling Concur (Class C b.1 by analogy, `10-appendixd.md` line 157 "simulators, emulators, or facilities used to test Class A, B, or C software"; Class E a.4 line 239 and c.6 line 259 verified); `traceability.py`, `validate_docs.py` Concur (Class D a.1(b) line 183, example b.1 line 207 "project assurance databases ... requirements management databases"; Class E c.5, c.6); render tools Concur; gate scripts Concur; release tooling Concur (b.1 "integrated build management systems"); simulation decks Concur (b.1 "engineering design and modeling tools"); `tools/refs/*.py` Concur with finding-5 (example b "file format converters", line 243) | `10-appendixd.md` lines 175 to 262 |
| CL-3 | Differences: finding-3 (criterion a missing on nine hazards with Software-class causes); the recorded criteria b, c, e are supported by each hazard's `firmware_role.statement` and controls | `hazards.json` 0.4.0-pha `causes[].class == "Software"` and `firmware_role`; SWEHB `7-02` line 58 onward (para 3.2 a to e) |
| CL-4 | Differences listed: criteria, `safety_critical`, component strings and `swe134_items` of all 15 hazards equal 03 section 4.2 verbatim; version, hash and the `control_req_ids` of the Software controls differ (finding-2) | Script comparison of 03 section 4.2 against `hazards.json` 0.4.0-pha, 2026-09-25 |
| CL-5 | Per component: every criteria cell equals the recomputed union (PA enable and TX sequencer a, b, c, e over HZ-001, 003, 004, 006, 007, 008, 011, 012; keyer a, b, c over HZ-001, 004, 010; safe-state manager a, b, c, e; scheduler and runtime a, b, c, e; battery and charging supervision c, e; thermal b, e; audio limiter b, c; frequency-word path and verification unit a, c, e). The two HZ-008 type (ii) findings state hazard, criterion and reason. Dissent: menu handling meets criterion a for HZ-001, HZ-004, HZ-005 and HZ-006 and has no row and no type (ii) finding (finding-1) | Union recomputed from `hazards.json` 0.4.0-pha; 03 section 4.3 |
| CL-6 | Concur | Section 4.3.1 makes the determination SWEHB `7-02` line 70 requires ("including verification of safety-critical software"); "none" basis stated; rigor assigned per item. Spot-checked: `tools/tests/test_traceability.py` asserts `HAZARD_REQ_NOT_TESTED` (5 references), `HAZARD_UNRESOLVED` (1) and `VERIFIED_WITHOUT_EVIDENCE` (1); `tools/traceability.py --report-only` exits 0 |
| CL-7 | Concur with INSP-009 finding-1 (see concurrence table); otherwise Concur | App. A `07-appendixa.md` line 71 verified verbatim as quoted in 03 section 4.1 step 4. REQ-SYS-136 restores Iambic A at every configuration reset and REQ-SYS-056 changes the key-input mode only by menu selection, so a menu fault can remove straight-key operation, a SI-018 core capability |
| CL-8 | Differences not listed: the 0.4.0-pha differences (finding-2). 07 differences: concur with INSP-009 finding-2 that X1 and X3 list items already resolved in the current 07 (07 section 14.1 now carries HZ-008 in `SW-TXSEQ`, the `SW-SAFE` union a, b, c, e, fault annunciation and the no-gap watchdog, WP-SW-14), and that the remaining 07 difference (the HZ-008 type (ii) findings are not in 07 `SW-SAFE` and `SW-SCHED`; the stale 07 note "03 section 4.3 records a, c until its re-transcription") is not listed | 07 section 14.1 blob `1b8864b` |
| CL-9 | Dissent | Classification: concur with the Class A election and the per-item letter classes except the justification gap of finding-4. Safety-critical determination: dissent on the exclusion of the menu override path (finding-1, Major); the rest concurs. Mission-critical set: dissent per INSP-009 finding-1 |

## Plan review items (CK-REQ-G1 to G8; 08 section 3.1, 07 section 2.1.1)

| # | Answer | Evidence |
|---|---|---|
| CK-REQ-G1 | No | Contradictions with the charter (concur with INSP-009 finding-4: charter lines 156, 159 and 167 already carry what 03 items c, e and h propose, and 03 section 6.3 misstates them) and the single-record statement of section 3.3 against the two assessment records (finding-6). No contradiction with `rmm.json` or the compliance matrix found |
| CK-REQ-G2 | Yes | 0 occurrences of "TBD", "as appropriate" or "should consider"; 0 em dashes in 03, `rmm.json`, `rmm.md`; every step names its artifact (sections 4.1, 6.4) |
| CK-REQ-G3 | Yes | Roles and approval points: sections 2, 6.2, 7, 9 (owner as ETA, SMA TA, HMTA, CIO/SAISO designee; signature block) |
| CK-REQ-G4 | Yes | T and NA rows mirrored per section 6.4 item 4 into 07 section 22 and SEMP section 9.0 (SWE-121 row) |
| CK-REQ-G5 | N/A | 03 carries no cybersecurity section of its own; section 7 records the authority for the section 3.11 rows, whose content is 07 section 16 |
| CK-REQ-G6 | N/A | 03 defines no measurement; SWE-089 fields are in this record |
| CK-REQ-G7 | Yes, with the stale note of finding-2 | Section 6.1 claims re-run: `render_rmm.py --check` exit 0, 20 `test_render_rmm` tests pass, full suite 331 tests with 1 failure (`test_validate_docs.RepositoryTests.test_repository_exit_zero`), as stated; the stated cause is now incomplete (finding-2) |
| CK-REQ-G8 | Yes | Verified in the corpus: SWE-020, SWE-176 (`03-chapter3.md` lines 149, 155), SWE-205 (line 183), SWE-134 (line 187), SWE-139, SWE-121, SWE-125 (lines 67 to 71), SWE-126 at 2.1.8.2, SWE-150 at 2.2.7, SWE-021 at 2.2.8, SWE-223 at 2.1.2.7, CHMO at 2.1.4.2, SAISO signature at 2.2.6, relief Note at 2.2.5 (`02-chapter2.md`); Table 2 counts recomputed from `09-appendixc.md`: 100 Class A, 64 Class D, 12 Class E rows, the Class E set equal to the 12 SWE ids 03 section 6.1.1 lists, no Class D or E row outside Class A, no Class A row outside Class B; SWEHB `7-02` quote at line 70 verbatim |

## Software assurance tasks applied (SWEHB section 7.1 tabs)

| Id | Task (SWEHB page, section 7.1) | Result | Evidence |
|---|---|---|---|
| SA-020-1 | swe-020 task 1: perform a classification or concur with engineering's | Concur with the classification except finding-4 | CL-1, CL-2 |
| SA-125-1 | swe-125 task 1: confirm the project maintains a requirements mapping matrix for all NPR 7150.2 requirements | Yes | `rmm.json` 100 rows equal the Class A set; `render_rmm.py --check` exit 0; one matrix covers every software item (03 section 6.1.1) |
| SA-125-2 | swe-125 task 2: maintain the mapping matrix for NASA-STD-8739.8 requirements | Not performable; recorded | The standard is not in the corpus (charter section 1 row 6); relief carried by the SWE-022 and SWE-023 T rows, whose `residual_risk` names the gap, and 03 section 6.4 item 8 (owner obtains the standard before PDR; CR re-dispositions both rows). No finding |
| SA-139-1 | swe-139 task 1: assess compliance with the NPR per class and safety criticality | Yes, with finding-5 | Every Class A row addressed; T and NA rows carry `tailoring_rationale` and `residual_risk` (schema-enforced); status rule enforced by `render_rmm.py`; applicability basis depends on converter output (finding-5) |
| SA-176-1 | swe-176 task 1: confirm classification and RMM records are maintained for the life of the project | No | Mechanism in git stated (03 section 6.4 item 6); product blobs not yet committed (package item H17); the assessment records are not all named (finding-6) |
| SA-205-1 | swe-205 task 1: hazard reports contain all known software contributions by action, inaction or incorrect action | No | finding-1 (menu override path), finding-3 (criterion a) |
| SA-205-2 | swe-205 task 2: hazard reports identify the software components per the criteria | Yes, except finding-1 | `firmware_role.components` present for all 12 hazards with a role; HZ-008 type (ii) findings carried as X14 |
| SA-205-3 | swe-205 task 3: hazard analyses identify the software components per the criteria | Yes, except finding-1 | `hazard-analysis.md` section 6.2 generated from `hazards.json` 0.4.0-pha by the 03 section 4.1 rule |
| SA-205-4 | swe-205 task 4: traceability between software requirements and hazards exists | Yes | `hazards.json` `requirement_ids` equals the union of `control_req_ids` for all 15 hazards (script check, 2026-09-25); `traceability.py --report-only` exit 0; 03's statement that the L2 links wait for PDR is stale (finding-2) |
| SA-134-4 | swe-134 task 4: design isolates safety-critical elements and data from non-safety-critical ones | No | finding-1: no isolation stated between menu handling and the SWE-134 d override checks |
| SA-134-6 | swe-134 task 6: the SWE-134 implementation is consistent with the system hazard analysis | Yes | 03 section 5 Hazards column recomputed from `swe134_items` of 0.4.0-pha: equal for all 12 items a to l; provision text points to `hazard-analysis.md` section 7 |

## Concurrence with INSP-009 (the paired peer review of the same blobs)

| INSP-009 finding | Severity there | Assurance result | Evidence re-checked |
|---|---|---|---|
| finding-1 (menu key-input mode selection not mission-critical) | Major | Concur | REQ-SYS-056, REQ-SYS-136, REQ-SYS-163 statements; App. A line 71. Distinct from finding-1 here, which concerns the safety-critical override path |
| finding-2 (X1, X3 stale against the current 07) | Minor | Concur | 07 section 14.1 blob `1b8864b` |
| finding-3 (inventory omits `tools/render_review_figures.py`, figure generators, TV evidence scripts, `tools/tests/*.py`) | Minor | Concur | `tools/render_review_figures.py` exists; 0 occurrences of it in 03 |
| finding-4 (charter items c, e, h already in the charter) | Minor | Concur | Charter lines 156, 159, 167 |
| finding-5 (criterion a on HZ-001, 003, 006, 012) | Minor | Concur and extended by finding-3 here | `hazards.json` Software-class causes |
| finding-6 (formal risk acceptance by other operators, NPR 7150.2D section 2.2.1) | Minor | Concur | `02-chapter2.md` section 2.2.1 text |

## Tool runs (2026-09-25, this reviewer)

| Command | Exit | Result |
|---|---|---|
| `.venv/bin/python tools/validate_docs.py` | 1 | 31 passed, 2 failed (`allocation.json`, `measurements.json`: schema not found); all peer-review records PASS |
| `.venv/bin/python tools/traceability.py --report-only` | 0 | |
| `.venv/bin/python tools/render_risk.py --check --gate SRR --hazards docs/safety/hazards.json` | 0 | 65 risks, 159 candidates, 0 warnings, hazard cross-check |
| `.venv/bin/python tools/render_rmm.py --check` | 0 | 100 rows; FC 75, T 17, NA 8; `rmm.md` current |
| `.venv/bin/python tools/render_compliance.py --check` | 0 | validation passed, rendered file current |
| `.venv/bin/python -m unittest discover -s tools/tests` | 1 | 331 tests, 1 failure (`test_repository_exit_zero`, same cause as the validate_docs failures) |

## Participants

Author `author:classification-rmm` (not present). Peer reviewer `reviewer:classification` (INSP-009). Software assurance reviewer `sa-reviewer:classification` (this record; distinct invocation from the author and from the INSP-009 reviewer, 07 section 2.1.1 row "Software plans"). Owner: resolves the findings as ETA and SMA TA in the SRR decision memo section 11 (03 section 3.3).

## Measurements (SWE-089)

Items checked: 3 readiness, 9 CL, 8 CK-REQ-G, 11 SA tasks (31). Items answered No or Dissent at iteration 1: 11 (front matter `items_no`). Findings: Major 1, Minor 6 (finding-7 raised at iteration 2); fixed and verified 5; open 2; deferred 0. Iteration 2. Effort, cumulative: 62 turns, 80 minutes (iteration 1: 42 turns, 55 minutes).

## Closure verification (iteration 2, 2026-09-26)

Same role (`sa-reviewer:classification`), new invocation; the product was not edited by this reviewer. Input: the author's fix list for INSP-017 finding-1 to finding-6 (no disputes). Each finding was checked against the fourth revision of 03 (blob `fdc8d07`), `rmm.json` (blob `78c3b23`) and `rmm.md` (blob `fe374a4`); search-first: `mcp__claude-context__search_code` on `/Users/robinonsay/rust/cwht` ran before any `grep`, which was then used only to pin the SWEHB `swe-134` task 4 b line, the 07 "Neither" paragraph and the INSP-009 front matter.

| Finding | Iteration 1 severity | Result | Evidence |
|---|---|---|---|
| finding-1 | Major | Closed | 03 sections 4.3 (row and type (ii) paragraph), 4.4, 5 rows a, d, e, g, i, 6.5 items X1 (x), (xi), X16, X20, section 9; `rmm.json` SWE-023, SWE-134, SWE-205 |
| finding-2 | Minor | Closed | Script comparison of section 4.2 against `hazards.json` 0.4.0-pha: 0 differences in controls, `control_req_ids` and components; hash equal; section 6.1 note complete |
| finding-3 | Minor | Open | `hazards.json` unchanged; tracked by 03 item X15 |
| finding-4 | Minor | Closed | 03 section 3.1 `cwht-fw` row and conclusion; `rmm.json` meta |
| finding-5 | Minor | Closed | 03 section 3.1 converter rows, section 6.1.1, item X17 |
| finding-6 | Minor | Closed | 03 section 3.3 table, section 9, signature line; `rmm.json` SWE-020, SWE-176, SWE-205; template and INSP-009 front matter are item X19 |
| finding-7 | Minor (new) | Open | 03 section 4.2 difference item 2 says 13 controls, lists 19 |

Assurance task results changed by the fixes: SA-134-4 Yes (the determination now treats the override command path as safety-critical, and the isolation alternative is stated with its verification case); SA-205-1 still No for finding-3 only; SA-205-2 and SA-205-3 Yes for 03, with `hazards.json` and `hazard-analysis.md` to follow through X16; SA-176-1 Yes for 03 and the RMM (commit of the blobs is SRR package item H17); SA-139-1 Yes; SA-020-1 Concur. CL-9: concur with the classification and with the safety-critical and mission-critical determinations as now proposed, subject to the owner's ruling on X20 and decision 9. R3 now Yes (03 states 0.4.0-pha and its hash, and lists the differences).

Concurrence spot check of the INSP-009 fixes the author lists (for the INSP-009 reviewer, who dispositions them): the key-input and keyer-mode selection path is in the section 4.3 mission-critical paragraph with the REQ-SYS-136 argument and in section 5 rows a, b, g; the inventory and placement rule of section 1 name `tools/render_review_figures.py`, the SRR figure scripts, `render_icd_figures.py`, the TV evidence scripts and `tools/tests/`; section 6.3 cites charter lines 156, 159 and 167 as they read at `4e3f891` (re-read in full this invocation); section 4.4 has the three-group risk-taker table and section 9 the spokesperson line. No disagreement found.

Open items outside the product that this closure depends on (cross, already in 03 section 6.5): X1 (07 section 14.1 still places all menu handling under "Neither", line 597, and cites 0.3.0-pha), X15, X16, X17, X19, X20.

### Tool runs (2026-09-26, this reviewer)

| Command | Exit | Result |
|---|---|---|
| `.venv/bin/python tools/validate_docs.py` | 1 | 34 passed, 2 failed (`docs/design/allocation.json`, `docs/plan/measurements.json`: schema not found); this record and INSP-009 PASS after the iteration 2 edit |
| `.venv/bin/python tools/traceability.py --report-only` | 0 | |
| `.venv/bin/python tools/render_risk.py --check --gate SRR --hazards docs/safety/hazards.json` | 0 | `register.md` current |
| `.venv/bin/python tools/render_rmm.py --check` | 0 | 100 rows; FC 75, T 17, NA 8; In place 37; `rmm.md` current |
| `.venv/bin/python tools/render_compliance.py --check` | 0 | validation passed, rendered file current |
| `.venv/bin/python -m unittest discover -s tools/tests` | 1 | 331 tests, 1 failure (`test_repository_exit_zero`, same cause as the validate_docs failures) |

### Closure block

- Findings: 7, one of them of the higher severity and six Minor; none of the higher severity remains unresolved.
- Closed and verified: 5 (finding-1, 2, 4, 5, 6). Still open: 2 Minor (finding-3, tracked by X15, fix owned by the hazard analysis author; finding-7, new). Disputed: 0. Deferred: 0.
- Verdict at iteration 2: NEEDS CHANGES (two Minor findings open; no Major open). The record closes when finding-7 is corrected and finding-3 is closed or the owner defers it to a RID with a closure plan in the SRR decision memo.
- `record_status` stays Open (set to Closed by the lead SE); `date_closed` null.

## Verdict

```
VERDICT: NEEDS CHANGES (iteration 2)
FINDINGS:
- [Major] finding-1 CL-5/CL-9: Closed; menu override command path proposed safety-critical by type (ii) finding, owner rules at X20.
- [Minor] finding-2 R3/CL-4: Closed; section 4.2 re-transcribed from 0.4.0-pha, 0 differences.
- [Minor] finding-3 CL-3: still open; criterion a missing on nine hazards in hazards.json (X15).
- [Minor] finding-4 CL-2: Closed; cwht-fw "no class fits by the letter; Class D is the floor".
- [Minor] finding-5 CL-2: Closed; NODIS converters Class D, X17 for the TV record.
- [Minor] finding-6 SWE-176: Closed; both records named; template and INSP-009 front matter via X19.
- [Minor] finding-7 CL-4: new; section 4.2 difference item 2 says 13 controls, lists 19.
ITEMS N/A: CK-REQ-G5, CK-REQ-G6
MEASUREMENTS: size=9 inventory rows, 13 per-item rows, 11 components; turns=62; minutes=80; major=1; minor=6
```
