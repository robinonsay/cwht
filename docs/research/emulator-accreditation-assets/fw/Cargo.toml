[package]
name = "emutest"
version = "0.1.0"
edition = "2024"

# Standalone: do not join any parent workspace.
[workspace]

[dependencies]

[profile.dev]
panic = "abort"
opt-level = 1
[profile.release]
panic = "abort"
opt-level = 2
debug = true
lto = false
